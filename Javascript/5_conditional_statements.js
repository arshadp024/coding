let a = prompt("Enter your age");           //It will show error is local IDE  but works on browser. 
console.log(typeof(a));                     //The prompt function stores the value in only string data type
                                            // Prompt can be used without assigning in the variable 
if(a > 0){
    alert("This is a valid age");
} else { 
    alert("This is a valid age");
}

//todo:  The alert() function is used to show a message to the user in a small pop-up window.
//todo:  The prompt() function is used to take input from the user.
